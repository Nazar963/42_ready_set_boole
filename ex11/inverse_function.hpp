#ifndef INVERSE_FUNCTION_HPP
#define INVERSE_FUNCTION_HPP

#include <cstdint>
#include <vector>
#include <iostream>
#include <algorithm>

std::vector<uint16_t> inverse_function(double n);
uint16_t dispalmare_bit(uint32_t k, bool xory);

#endif